[C]
as = "sh"
cmd = "code"

[O]
cmd = "obs"

[S]
as = "dash"
cmd = "cd ~/repos/sticker-selector && ./sticker-selector"

[T]
cmd = "cactus"
args = ["--binds", '{{ env "XDG_CONFIG_HOME" }}/cactus/binds-terminal-apps.toml']

[N]
cmd = "kitty"
args = ["--title", "Popup Terminal"]
