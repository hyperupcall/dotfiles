========
dotfiles
========

contains my dotfiles; maintained with baobab (a custom tool since neither dotdrop, chezmoi, GNU stow, nor rcm did exactly what i wanted)

also see `gh/eankeen/globe`

## File Structure

├─ global    dotfiles at /
├─ user     dotfiles at ~
├─ local   dotfiles at .
└─ scripts
  └─ ./\*: scripts added to the PATH
  └─ dots: scripts just for this dotfiles repository
  └─ globe: scripts hidden globally, but available via `run.sh`

dotfiles located in 'project' are managed by eankeen/globe

todo: enable readline C-s
