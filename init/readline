# Case insensitive auto-completion (see  https://github.com/kwbr/etc-bash/blob/master/.inputrc)
bind 'set completion-ignore-case on'

# Case insensitive globs
# shopt -s nocaseglob

# Complete from any part of the string, not just the end of the line.
bind 'set skip-completed-text on'

# Prevent extra tab just to show auto-completion lists (http://lwn.net/Articles/568854)
# bind 'set show-all-if-ambiguous on'

