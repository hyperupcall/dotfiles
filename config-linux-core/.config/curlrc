# Maximum time in seconds that you allow curl's connection to take. Only limits the connection
# phase, so if curl connects within the given period it continues; if not, it exits.
connect-timeout = 15
